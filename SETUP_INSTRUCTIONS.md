# 🚀 Инструкция по настройке Telegram Analytics

## Шаг 1: Настройка Supabase

### 1.1 Создать таблицу в базе данных

1. Зайди в свой проект Supabase: https://supabase.com
2. Перейди в **SQL Editor** (слева в меню)
3. Скопируй и выполни SQL из файла `database/schema_simple.sql`
4. Нажми **Run** (или Ctrl+Enter)

### 1.2 Проверить что таблица создана

1. Перейди в **Table Editor** (слева в меню)
2. Должна появиться таблица `telegram_conversations`
3. Проверь что есть все колонки:
   - id
   - manager_id
   - manager_name
   - client_telegram_id
   - message_time
   - message_type
   - is_new_client
   - channel_source
   - response_time_minutes
   - message_text
   - created_at

### 1.3 Получить API ключи

1. Перейди в **Settings** → **API**
2. Скопируй:
   - **Project URL** (например: https://xxxxx.supabase.co)
   - **anon public key** (длинный ключ)

---

## Шаг 2: Настройка Userbot на VPS

### 2.1 Проверить что .env файл настроен


```bash
cd ~/telegram-analytics
cat .env
```

```
SUPABASE_URL=https://твой-проект.supabase.co
SUPABASE_KEY=твой_anon_public_key
```

```bash
nano .env
```


### 2.2 Добавить менеджеров


```bash
cd ~/telegram-analytics
source venv/bin/activate
python scripts/add_manager.py
```

- Номер телефона менеджера (например: +79991234567)
- Имя менеджера (например: Иван)

- Введи код из Telegram
- Если включена 2FA - введи пароль


### 2.3 Проверить что userbot работает

```bash
sudo systemctl status telegram-analytics
```


```bash
sudo journalctl -u telegram-analytics -f
```

---

## Шаг 3: Настройка CRM на Netlify

### 3.1 Добавить переменные окружения в Netlify

1. Зайди на Netlify: https://app.netlify.com
2. Открой свой сайт (docsteam)
3. Перейди в **Site settings** → **Environment variables**
4. Добавь 2 переменные:

   **NEXT_PUBLIC_SUPABASE_URL**
   ```
   https://твой-проект.supabase.co
   ```

   **NEXT_PUBLIC_SUPABASE_ANON_KEY**
   ```
   твой_anon_public_key
   ```

5. Нажми **Save**

### 3.2 Передеплоить сайт

1. В Netlify перейди в **Deploys**
2. Нажми **Trigger deploy** → **Deploy site**
3. Дождись завершения деплоя (2-3 минуты)

---

## Шаг 4: Проверка работы

### 4.1 Проверить что данные пишутся в Supabase

1. Зайди в Supabase → **Table Editor** → `telegram_conversations`
2. Обнови страницу
3. Должны появляться новые строки когда менеджеры получают/отправляют сообщения

### 4.2 Проверить CRM

1. Зайди на сайт: https://docsteam.netlify.app
2. Логин: demo, пароль: demo
3. Перейди в **CRM** (пароль: srm2026)
4. Открой вкладку **Telegram**
5. Должна появиться статистика!

---

## 🐛 Troubleshooting

### Данные не появляются в CRM

1. Проверь что userbot работает: `sudo systemctl status telegram-analytics`
2. Проверь логи: `sudo journalctl -u telegram-analytics -n 50`
3. Проверь что в Supabase есть данные в таблице
4. Проверь переменные окружения в Netlify

### Userbot не запускается

1. Проверь .env файл
2. Проверь что добавлены менеджеры: `ls ~/telegram-analytics/sessions/`
3. Перезапусти: `sudo systemctl restart telegram-analytics`

### Нет данных в Supabase

1. Проверь URL и ключ в .env
2. Проверь права доступа к таблице (должен быть отключен RLS)
3. Проверь логи userbot

---

## ✅ Готово!

- Userbot собирает данные из Telegram
- Данные сохраняются в Supabase
- CRM показывает красивую аналитику!

