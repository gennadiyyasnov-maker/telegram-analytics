# 🤖 Telegram Analytics Userbot

Автоматическая система сбора статистики переписок менеджеров с клиентами в Telegram.

## 📋 Описание

Система из 20 userbot'ов, которые отслеживают переписки менеджеров с клиентами и собирают аналитику:
- Количество новых клиентов
- Количество повторных клиентов
- Время первого ответа
- Источник (канал)
- Активность менеджеров

## 🏗️ Архитектура

```
Telegram (20 менеджеров)
    ↓
20 Userbot процессов (Python + Telethon)
    ↓
Supabase Database
    ↓
CRM Dashboard (Next.js)
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env файл
```

### 3. Добавление менеджера

```bash
python scripts/add_manager.py
```

### 4. Запуск системы

```bash
python main.py
```

## 📊 Собираемые метрики

- **new_clients** - новых клиентов за день
- **returning_clients** - повторных клиентов
- **total_conversations** - всего переписок
- **avg_response_time** - среднее время ответа (минуты)
- **messages_sent** - отправлено сообщений
- **messages_received** - получено сообщений
- **channel_source** - откуда пришел клиент

## 🔧 Управление

### Проверить статус всех userbot'ов
```bash
python scripts/status.py
```

### Добавить нового менеджера
```bash
python scripts/add_manager.py
```

### Удалить менеджера
```bash
python scripts/remove_manager.py
```

### Посмотреть логи
```bash
python scripts/logs.py --name "Иван"
```

### Live статистика
```bash
python scripts/live_stats.py
```

## 📁 Структура проекта

```
telegram-analytics/
├── config/             # Конфигурация
│   ├── settings.py
│   └── supabase.py
├── core/               # Основная логика
│   ├── userbot_manager.py
│   ├── message_analyzer.py
│   └── statistics.py
├── managers/           # Данные менеджеров
│   └── sessions/       # Session файлы (не в Git)
├── scripts/            # Утилиты управления
│   ├── add_manager.py
│   ├── status.py
│   └── deploy.sh
├── main.py             # Точка входа
├── requirements.txt    # Python зависимости
└── .env.example        # Пример конфигурации
```

## 🔒 Безопасность

- ✅ Session файлы НЕ коммитятся в Git (.gitignore)
- ✅ Userbot работает в режиме "только чтение"
- ✅ Менеджеры могут отключить в любой момент
- ✅ Все данные шифруются

## 🛠️ Требования

- Python 3.9+
- Ubuntu 20.04+ (или любой Linux)
- 2GB RAM минимум
- Telegram API credentials

## 📝 Получение API credentials

1. Зайти на https://my.telegram.org
2. Авторизоваться
3. API development tools
4. Создать приложение
5. Получить `api_id` и `api_hash`

## 🐛 Troubleshooting

### Userbot не подключается
```bash
# Проверьте логи
tail -f logs/userbot_MANAGER_NAME.log
```

### Нет данных в CRM
```bash
# Проверьте подключение к Supabase
python scripts/test_supabase.py
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи (`logs/`)
2. Запустите `python scripts/status.py`
3. Проверьте переменные окружения в `.env`

## 🔄 Обновление

```bash
git pull
pip install -r requirements.txt
sudo systemctl restart telegram-analytics
```

## 📄 Лицензия

Private - только для внутреннего использования.
